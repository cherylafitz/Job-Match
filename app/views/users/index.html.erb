<div class="resume-page">
<h2><%= @current_user.name.titlecase %>'s Resume</h2>
<%= link_to "<span class='glyphicon glyphicon-arrow-left'></span> back".html_safe, :back, class: "icon-pad pull-left" %>
<div class="row">
  <p class="text-muted instructions">This resume is used to generate a Match Score with the jobs you select, so be sure it's up to date!</p>
  <div class="col-md-6 col-lg-6">
    <% if @current_user.resume %>
      <%= link_to '', users_edit_path, class: 'icon-pad btn btn-primary btn-xs glyphicon glyphicon-pencil pull-right' %>
    <% else %>
      <%= link_to '', users_edit_path, class: 'icon-pad btn btn-success btn-xs glyphicon glyphicon-plus pull-right' %>
    <% end %>
    <% if @current_user.resume %>
      <p><%= @current_user.resume %></p>
    <% end %>
  </div>
  <div class=" col-md-6 col-lg-6">
    <div class="resume my-cloud"></div>
    <h4>About Match Scores</h4>
    <p class="instructions">Match Scores are generated by assessing how many key words are shared between
  your resume and a job posting, adjusted to account for the length of the posting. Match Scores can be found on the  <%= link_to "Job Board", profile_path, class: "" %> </p>
  </div>
</div>
<div class='clearfix'></div>
</div>
